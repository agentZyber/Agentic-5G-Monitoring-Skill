apiVersion: v1
kind: Service
metadata:
  name: {{ include "zortenet.fullname" . }}
  labels:
    {{- include "zortenet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "zortenet.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
