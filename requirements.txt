# Runtime dependencies (imported by src/). All Apache-2.0 / MIT / BSD-3-Clause.
fastapi
uvicorn[standard]
pydantic
requests
sse-starlette
evolved5g==0.2.6          # NEF/CAPIF SDK (EVOLVED-5G) — Apache-2.0

# Agentic / RAG layer
langchain-openai
langgraph
chromadb
sentence-transformers
mcp                       # MCP server faces for the toolkit app (zortenet.app) in the same image

# Optional: reserved for the usage-metering layer (not yet imported)
# redis

# Test dependencies
pytest
pytest-asyncio
httpx
